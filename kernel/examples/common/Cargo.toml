[package]
name = "common"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
delta_kernel = { path = "../../../kernel", features = [
  "arrow-55",
  "default-engine",
  "internal-api",
] }
